<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ shop.name }}</title>
  <script>
    // Redirect to headless storefront except for Shopify-native routes like /cart and /checkout
    (function() {
      var path = window.location.pathname;
      var allow = [/^\/cart(\/|$)/, /^\/checkout(\/|$)/, /^\/account(\/|$)/];
      var isAllowed = allow.some(function(re){ return re.test(path); });
      if (!isAllowed) {
        var dest = 'https://aircaresupplyco.com' + window.location.pathname + window.location.search + window.location.hash;
        window.location.replace(dest);
      }
    })();
  </script>
</head>
<body>
  <div style="text-align: center; padding: 50px; font-family: Arial, sans-serif;">
    <h1>Redirecting to {{ shop.name }}...</h1>
    <p>If you are not redirected automatically, <a href="https://aircaresupplyco.com">click here</a>.</p>
  </div>
</body>
</html>